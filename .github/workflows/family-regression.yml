name: Family Regression

# Real-model regression net: for every public model family, pull the smallest
# checkpoint at its smallest quant from the public catalog (real user chain,
# signature-verified) and transcribe a committed fixture on the CPU backend,
# comparing against committed goldens (tooling/family-regression/goldens/).
#
# Nightly and published-release jobs remain CPU-only/post-release monitoring.
# They cannot authorize catalog or release activation. The reusable
# `workflow_call` with `pre_publication: true` is the candidate blocker:
# release-core must call it with immutable candidate and staging-catalog bytes
# before deploying the PublishedInert catalog. GPU correctness is a separate,
# post-publication exact-target activation gate.

on:
  # This reusable entrypoint is the pre-publication contract seam. The release
  # orchestrator must call it with staged candidate artifacts before catalog
  # activation; the release event below remains monitoring only.
  workflow_call:
    inputs:
      pre_publication:
        description: "Require a release-candidate family regression contract"
        required: true
        type: boolean
      release_tag:
        description: "Exact draft/public vX.Y.Z release whose CLI is under test"
        required: false
        type: string
  workflow_dispatch:
  schedule:
    - cron: "23 4 * * *"
  release:
    types: [published]

concurrency:
  group: family-regression-${{ github.ref }}
  cancel-in-progress: false

permissions:
  attestations: read
  # Pre-publication runs download a GitHub *draft* release. Drafts are
  # invisible to contents:read, so this workflow needs write to see them.
  contents: write
  packages: read

defaults:
  run:
    shell: bash

env:
  CARGO_TERM_COLOR: always

jobs:
  pre-publication-contract:
    name: Pre-publication family regression contract
    if: ${{ inputs.pre_publication == true && (github.event_name != 'release' || startsWith(github.event.release.tag_name, 'v')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify exact draft CLI provenance and committed staging catalog
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          set -euo pipefail
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          version="${RELEASE_TAG#v}"
          mkdir -p staging/candidate
          asset="openasr-${version}-linux-x86_64.tar.gz"
          python3 tooling/release-manifest/gh_release.py download \
            "$RELEASE_TAG" staging/candidate "$asset" SHA256SUMS
          python3 tooling/release-manifest/release_asset_verifier.py \
            --asset "staging/candidate/$asset" --checksums staging/candidate/SHA256SUMS
          git fetch --no-tags origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
          source_digest="$(git rev-parse "${RELEASE_TAG}^{}")"
          gh attestation verify "staging/candidate/$asset" \
            --repo "$GITHUB_REPOSITORY" \
            --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/release-binaries.yml" \
            --source-digest "$source_digest"
          python3 -m json.tool model-registry/catalog.json >/dev/null
          sha256sum model-registry/catalog.json model-registry/catalog.signature.json
          echo "exact release CLI and committed staging catalog verified"

  build-cli-linux:
    name: Build CLI (ubuntu-latest)
    # The public repository also hosts desktop-vX.Y.Z releases. Those do not
    # carry open-core CLI assets and must skip this workflow without a red run.
    if: >-
      ${{ github.event_name != 'release' || startsWith(github.event.release.tag_name, 'v') }}
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/quintinshaw/openasr-ci-linux@sha256:8ddcfdea6a758bab0c14a9108de9b3876d23f44bcf815db109593b370f8b1c29
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
      - name: Resolve and fetch published release CLI
        id: published
        env:
          GH_TOKEN: ${{ github.token }}
          ASSET_SUFFIX: linux-x86_64
          RELEASE_TAG: ${{ inputs.release_tag || github.event.release.tag_name }}
        run: |
          set -uo pipefail
          available=false
          tag="${RELEASE_TAG}"
          if [ -z "${tag}" ]; then
            tag="$(gh api "repos/${GITHUB_REPOSITORY}/releases/latest" --jq .tag_name)"
          fi
          version="${tag#v}"
          asset="openasr-${version}-${ASSET_SUFFIX}.tar.gz"
          workdir="${RUNNER_TEMP}/published-cli"
          mkdir -p "${workdir}"
          if python3 tooling/release-manifest/gh_release.py download \
              "${tag}" "${workdir}" "${asset}" SHA256SUMS --repo "${GITHUB_REPOSITORY}"; then
            python3 tooling/release-manifest/release_asset_verifier.py \
              --asset "${workdir}/${asset}" --checksums "${workdir}/SHA256SUMS" || exit 1
            gh attestation verify "${workdir}/${asset}" \
              --repo "${GITHUB_REPOSITORY}" \
              --signer-workflow "${GITHUB_REPOSITORY}/.github/workflows/release-binaries.yml" || exit 1
            if tar -xzf "${workdir}/${asset}" -C "${workdir}" \
              && bin="$(find "${workdir}" -type f -name openasr | head -n 1)" \
              && test -n "${bin}" \
              && cp "${bin}" "${workdir}/openasr" \
              && chmod +x "${workdir}/openasr" \
              && "${workdir}/openasr" --version; then
              available=true
            fi
          fi
          echo "available=${available}" >> "${GITHUB_OUTPUT}"
          if [ "${available}" != "true" ]; then
            if [ "${GITHUB_EVENT_NAME}" = "release" ]; then
              echo "::error::published release ${tag} is missing usable ${asset}; refusing a duplicate local build"
              exit 1
            fi
            echo "published ${asset} is not usable; falling back to a local release build for this diagnostic run"
          fi
      - name: Prepare pinned Linux build environment
        if: steps.published.outputs.available != 'true'
        run: |
          git submodule update --init --recursive
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          openasr-ci-verify
      - uses: dtolnay/rust-toolchain@1.95.0
        if: steps.published.outputs.available != 'true'
      - uses: Swatinem/rust-cache@v2
        if: steps.published.outputs.available != 'true'
        with:
          shared-key: family-regression-ubuntu-latest
      - name: Build openasr CLI
        if: steps.published.outputs.available != 'true'
        # The binary is shared with the case jobs via an artifact, and
        # GitHub's x86 runner fleet mixes CPU generations, so host-tuned ggml
        # kernels can SIGILL on a different case runner (same rule as
        # release-binaries.yml).
        env:
          OPENASR_GGML_NATIVE: "0"
        run: cargo build --release -p openasr-cli
      - name: Stage published release CLI
        if: steps.published.outputs.available == 'true'
        run: |
          mkdir -p target/release
          cp "${RUNNER_TEMP}/published-cli/openasr" target/release/openasr
          chmod +x target/release/openasr
      - uses: actions/upload-artifact@v7
        with:
          name: openasr-cli-ubuntu-latest
          path: target/release/openasr
          retention-days: 3

  build-cli-macos:
    name: Build CLI (macos-15)
    if: >-
      ${{ github.event_name != 'release' || startsWith(github.event.release.tag_name, 'v') }}
    runs-on: macos-15
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
      - name: Resolve and fetch published release CLI
        id: published
        env:
          GH_TOKEN: ${{ github.token }}
          ASSET_SUFFIX: macos-arm64
          RELEASE_TAG: ${{ inputs.release_tag || github.event.release.tag_name }}
        run: |
          set -uo pipefail
          available=false
          tag="${RELEASE_TAG}"
          if [ -z "${tag}" ]; then
            tag="$(gh api "repos/${GITHUB_REPOSITORY}/releases/latest" --jq .tag_name)"
          fi
          version="${tag#v}"
          asset="openasr-${version}-${ASSET_SUFFIX}.tar.gz"
          workdir="${RUNNER_TEMP}/published-cli"
          mkdir -p "${workdir}"
          if python3 tooling/release-manifest/gh_release.py download \
              "${tag}" "${workdir}" "${asset}" SHA256SUMS --repo "${GITHUB_REPOSITORY}"; then
            python3 tooling/release-manifest/release_asset_verifier.py \
              --asset "${workdir}/${asset}" --checksums "${workdir}/SHA256SUMS" || exit 1
            gh attestation verify "${workdir}/${asset}" \
              --repo "${GITHUB_REPOSITORY}" \
              --signer-workflow "${GITHUB_REPOSITORY}/.github/workflows/release-binaries.yml" || exit 1
            if tar -xzf "${workdir}/${asset}" -C "${workdir}" \
              && bin="$(find "${workdir}" -type f -name openasr | head -n 1)" \
              && test -n "${bin}" \
              && cp "${bin}" "${workdir}/openasr" \
              && chmod +x "${workdir}/openasr" \
              && "${workdir}/openasr" --version; then
              available=true
            fi
          fi
          echo "available=${available}" >> "${GITHUB_OUTPUT}"
          if [ "${available}" != "true" ]; then
            if [ "${GITHUB_EVENT_NAME}" = "release" ]; then
              echo "::error::published release ${tag} is missing usable ${asset}; refusing a duplicate local build"
              exit 1
            fi
            echo "published ${asset} is not usable; falling back to a local release build for this diagnostic run"
          fi
      - name: Initialize open-core submodules
        if: steps.published.outputs.available != 'true'
        run: git submodule update --init --recursive
      - uses: dtolnay/rust-toolchain@1.95.0
        if: steps.published.outputs.available != 'true'
      - uses: Swatinem/rust-cache@v2
        if: steps.published.outputs.available != 'true'
        with:
          shared-key: family-regression-macos-15
      - name: Build openasr CLI
        if: steps.published.outputs.available != 'true'
        env:
          OPENASR_GGML_NATIVE: "0"
        run: cargo build --release -p openasr-cli
      - name: Stage published release CLI
        if: steps.published.outputs.available == 'true'
        run: |
          mkdir -p target/release
          cp "${RUNNER_TEMP}/published-cli/openasr" target/release/openasr
          chmod +x target/release/openasr
      - uses: actions/upload-artifact@v7
        with:
          name: openasr-cli-macos-15
          path: target/release/openasr
          retention-days: 3

  family:
    name: ${{ matrix.case.name }} (${{ matrix.os }})
    needs: [build-cli-linux, build-cli-macos]
    if: >-
      ${{ github.event_name != 'release' || startsWith(github.event.release.tag_name, 'v') }}
    strategy:
      # A single family failure must stay readable and must not cancel the
      # rest of the net.
      fail-fast: false
      matrix:
        # macos pinned (not macos-latest, which migrates to macOS 26 from
        # 2026-06-15): the regression net's environment must not drift
        # under the alias.
        os: [ubuntu-latest, macos-15]
        # Family registry: one entry per public model family, smallest
        # checkpoint x smallest quant. ONBOARDING A NEW FAMILY IS NOT DONE
        # until it is registered here with a committed golden under
        # tooling/family-regression/goldens/<name>/ (generate it with
        # `tooling/family-regression/run.sh --case <name> --ref <ref>
        # --update-golden` on a local CPU run, then confirm both CI
        # platforms; add golden.<os>.txt variants when platforms disagree).
        #
        # mode=transcribe runs the real user chain (pull -> transcribe on the
        # CPU backend -> golden compare). mode=verify covers packs with no
        # batch transcribe path (pull -> ggml integrity probe only).
        # strategy: exact | normalized | wer:<threshold> (see compare.py).
        case:
          - { name: whisper, ref: "whisper-tiny:q4", mode: transcribe, strategy: exact }
          - { name: moonshine, ref: "moonshine-tiny:q8", mode: transcribe, strategy: exact }
          - { name: qwen, ref: "qwen3-asr-0.6b:q4", mode: transcribe, strategy: exact }
          - { name: sensevoice, ref: "sensevoice-small:q8", mode: transcribe, strategy: exact }
          - { name: dolphin, ref: "dolphin-cn-dialect-small:q4", mode: transcribe, strategy: exact }
          # Same dolphin family, base tier: covers the size-generalized runtime
          # (512 d_model import path) that the small checkpoint does not reach.
          - { name: dolphin-base-pack, ref: "dolphin-cn-dialect-base:q4", mode: transcribe, strategy: exact }
          - { name: xasr, ref: "xasr-zh-en:q8", mode: transcribe, strategy: exact }
          - { name: cohere, ref: "cohere-transcribe-03-2026:q8", mode: transcribe, strategy: exact }
          # Public MOSS q4 has the same JFK words as the measured fp16 CPU
          # baseline; this case exercises the released pull -> verify -> decode
          # chain. Speaker/timestamp structure remains covered by its dedicated
          # real-pack and weight-free contract tests.
          - { name: moss, ref: "moss-transcribe-diarize:q4", mode: transcribe, strategy: exact }
          # FireRedASR2-AED: catalog entry ships staged private (public:false
          # until the 0.1.9 batch, see models-core.toml), so this row starts
          # failing closed on the pull step until that flip -- matches
          # dolphin's own staged-then-flipped onboarding history.
          - { name: firered, ref: "firered-aed-l-v2:q4", mode: transcribe, strategy: exact }
          # Speaker separation is exercised end to end through --diarize on the
          # whisper-tiny pack; the golden asserts the transcript, not speaker
          # labels. Only the ReDimNet2-B6 embedder is pulled: it is the single
          # supported speaker-embedding pack (see requiredDiarizationModels /
          # registry.rs), and the retired WeSpeaker id this row used to pull no
          # longer exists in the catalog at all, so that pull failed closed.
          - { name: diarize, ref: "whisper-tiny:q4", mode: diarize, strategy: exact,
              args: "--extra-pull redimnet2-b6-cn:fp16" }
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          name: openasr-cli-${{ matrix.os }}
          path: bin
      - run: chmod +x bin/openasr
      - name: Restore model pack cache
        uses: actions/cache@v6
        with:
          path: ~/.openasr/models
          key: openasr-packs-${{ matrix.case.name }}-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('model-registry/catalog.json') }}
      - name: Run family regression case
        run: |
          tooling/family-regression/run.sh \
            --case "${{ matrix.case.name }}" \
            --ref "${{ matrix.case.ref }}" \
            --mode "${{ matrix.case.mode }}" \
            --strategy "${{ matrix.case.strategy }}" \
            --bin bin/openasr \
            ${{ matrix.case.args || '' }}
      - name: Upload transcript
        if: always() && matrix.case.mode != 'verify'
        uses: actions/upload-artifact@v7
        with:
          name: transcript-${{ matrix.case.name }}-${{ matrix.os }}
          path: tmp/family-regression/
          if-no-files-found: ignore
          retention-days: 14
