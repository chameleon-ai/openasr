# OpenASR publish/catalog core model data.
#
# Consumer-facing fields live here. Publish-only pipeline fields live in
# models-publish.toml and are merged by the publishing helper catalog reader.

["qwen3-asr-1.7b"]
family = "qwen"
hf_repo = "OpenASR/qwen3-asr-1.7b"
release_public = true
size = "1.7b"
registry_id = "qwen3-asr-1.7b"
display_name = "Qwen3-ASR 1.7B"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/Qwen/Qwen3-ASR-1.7B/blob/main/LICENSE"
license_class = "permissive"
# Explicit override: the family default from the generated Rust inventory folds all
# Chinese dialects into "zh". Qwen3-ASR's own model card enumerates 22 Chinese
# dialects (same 0.6B/1.7B checkpoint family), so this model is dialect-
# capable (see the generated family descriptor/inventory) and lists the family's 29 base
# languages plus every registered dialect it supports. zh-shanghai is listed
# on the strength of Qwen's own "Wu dialects" claim (the card's Wu-language
# coverage is presumed to include Shanghainese, its most prominent variety;
# not independently benchmark-verified per-city the way arXiv:2603.10420
# verifies FireRedASR2's dialects).
languages = [
    "ar", "cs", "da", "de", "el", "en", "es", "fa", "fi", "fil", "fr", "hi",
    "hu", "id", "it", "ja", "ko", "mk", "ms", "nan", "nl", "pl", "pt", "ro",
    "ru", "sv", "th", "tr", "vi", "wuu", "yue", "zh", "zh-anhui", "zh-dongbei",
    "zh-fujian", "zh-gansu", "zh-guizhou", "zh-hebei", "zh-henan", "zh-hubei",
    "zh-hunan", "zh-jiangxi", "zh-ningxia", "zh-shaanxi", "zh-shandong",
    "zh-shanghai", "zh-shanxi", "zh-sichuan", "zh-tianjin", "zh-yunnan",
    "zh-zhejiang",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
sort_weight = 920
recommended = true
upstream_release_date = "2026-01-28"

["qwen3-asr-0.6b"]
family = "qwen"
hf_repo = "OpenASR/qwen3-asr-0.6b"
release_public = true
size = "0.6b"
registry_id = "qwen3-asr-0.6b"
display_name = "Qwen3-ASR 0.6B"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/Qwen/Qwen3-ASR-0.6B/blob/main/LICENSE"
license_class = "permissive"
# Same explicit dialect override as qwen3-asr-1.7b above (identical 22-dialect
# card claim, same 0.6B/1.7B checkpoint family) -- see that entry's note.
languages = [
    "ar", "cs", "da", "de", "el", "en", "es", "fa", "fi", "fil", "fr", "hi",
    "hu", "id", "it", "ja", "ko", "mk", "ms", "nan", "nl", "pl", "pt", "ro",
    "ru", "sv", "th", "tr", "vi", "wuu", "yue", "zh", "zh-anhui", "zh-dongbei",
    "zh-fujian", "zh-gansu", "zh-guizhou", "zh-hebei", "zh-henan", "zh-hubei",
    "zh-hunan", "zh-jiangxi", "zh-ningxia", "zh-shaanxi", "zh-shandong",
    "zh-shanghai", "zh-shanxi", "zh-sichuan", "zh-tianjin", "zh-yunnan",
    "zh-zhejiang",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
sort_weight = 900
recommended = true
upstream_release_date = "2026-01-28"

["moonshine-tiny"]
family = "moonshine"
hf_repo = "OpenASR/moonshine-tiny"
release_public = true
aliases = ["moonshine"]
pull_alias = "moonshine"
size = "tiny"
registry_id = "moonshine-tiny"
display_name = "Moonshine Tiny"
license_name = "MIT"
license_source = "https://huggingface.co/UsefulSensors/moonshine-tiny"
license_class = "permissive"
quants = ["fp16", "q8_0"]
recommended_quant = "q8_0"
upstream_release_date = "2024-10-30"

["cohere-transcribe-03-2026"]
family = "cohere"
hf_repo = "OpenASR/cohere-transcribe-03-2026"
release_public = true
aliases = ["cohere-transcribe", "cohere"]
pull_alias = "cohere-transcribe"
size = "2b"
registry_id = "cohere-transcribe-03-2026"
display_name = "Cohere Transcribe 03-2026"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/CohereLabs/cohere-transcribe-03-2026/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0"]
recommended_quant = "q8_0"
upstream_release_date = "2026-03-24"

["whisper-large-v3-turbo"]
family = "whisper"
hf_repo = "OpenASR/whisper-large-v3-turbo"
release_public = true
aliases = ["whisper-turbo", "whisper-large-turbo"]
pull_alias = "whisper-turbo"
size = "turbo"
registry_id = "whisper-large-v3-turbo"
display_name = "Whisper Large v3 Turbo"
license_name = "MIT"
license_source = "https://github.com/openai/whisper/blob/main/LICENSE"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2024-10-01"

["whisper-small"]
family = "whisper"
hf_repo = "OpenASR/whisper-small"
release_public = true
aliases = []
pull_alias = "whisper-small"
size = "small"
registry_id = "whisper-small"
display_name = "Whisper Small"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-small/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["xasr-zh-en"]
family = "xasr-zipformer"
hf_repo = "OpenASR/xasr-zh-en"
release_public = true
size = "0.16b"
registry_id = "xasr-zh-en"
display_name = "X-ASR zh-en"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/GilgameshWind/X-ASR-zh-en/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0"]
recommended_quant = "q8_0"
sort_weight = 910
recommended = true
upstream_release_date = "2026-05-18"

# Parakeet TDT 0.6B v3 (NVIDIA FastConformer + Token-and-Duration Transducer,
# 25 European languages, CC-BY-4.0). Languages inherit
# The generated Rust inventory's parakeet-tdt family row (the 25-language set from NVIDIA's card,
# post-dialect-collapse; see _catalog.py).
["parakeet-tdt-0.6b-v3"]
family = "parakeet-tdt"
hf_repo = "OpenASR/parakeet-tdt-0.6b-v3"
release_public = true
aliases = []
pull_alias = "parakeet-tdt-0.6b-v3"
size = "0.6b"
registry_id = "parakeet-tdt-0.6b-v3"
display_name = "Parakeet TDT 0.6B v3"
license_name = "CC-BY-4.0"
license_source = "https://huggingface.co/nvidia/parakeet-tdt-0.6b-v3/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
sort_weight = 915
recommended = true
upstream_release_date = "2025-08-14"
# Forward-compat runtime floor: a new family requiring the 0.1.9 core runtime,
# same rationale/pin as firered-aed-l-v2.
min_core_version = "0.1.9"
min_cli_version = "0.1.9"

# Dolphin cn-dialect small (WeNet E-Branchformer, CTC + attention rescoring).
# Published PUBLIC to OpenASR/dolphin-cn-dialect-small: `release_public = true`
# routes the publish lane to a public HF repo and lets --public run the
# fail-closed public-listing gate and write the signed catalog.json entry,
# pinned to the immutable HF revision + per-pack sha256. Dolphin is
# dialect-capable (its executor ships a code->region-prompt map), so unlike other
# Chinese models it does NOT collapse regional dialects into a single `zh`:
# The generated Rust inventory's dolphin row advertises the base `zh` plus the registered province
# dialect codes (zh-sichuan, ...) as selectable source languages (see _catalog.py).
["dolphin-cn-dialect-small"]
family = "dolphin"
hf_repo = "OpenASR/dolphin-cn-dialect-small"
release_public = true
# Bare `dolphin-cn` now resolves here (Chinese-only dialect tier); bare
# `dolphin` resolves to the multilingual `dolphin-small` instead (see
# dolphin-small's own aliases/pull_alias below) -- the two pull-aliases were
# swapped 2026-07 so the unqualified id defaults to the multilingual model a
# user asking for plain "dolphin" almost certainly means, not the CN-only
# dialect tier.
aliases = ["dolphin-cn", "dolphin-cn-dialect"]
pull_alias = "dolphin-cn"
size = "small"
registry_id = "dolphin-cn-dialect-small"
display_name = "Dolphin CN-Dialect Small"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/DataoceanAI1/dolphi-cn-dialect-small/blob/main/README.md"
license_class = "permissive"
# Explicit Chinese-only override (same value as the generated dolphin inventory row
# already resolves to, pinned here so this model's language claim can never
# silently drift if the family default in _catalog.py changes) -- this is
# the "language=Chinese-only" marking, the same mechanism whisper's `*.en`
# checkpoints use (an explicit single/narrow `languages` override) rather
# than a family-agnostic flag.
languages = [
    "zh", "zh-anhui", "zh-guangdong", "zh-hebei", "zh-hubei", "zh-jiangsu",
    "zh-ningxia", "zh-shaanxi", "zh-shandong", "zh-shanghai", "zh-shanxi",
    "zh-sichuan", "zh-tianjin", "zh-tw",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "fp16"
sort_weight = 905
recommended = true
upstream_release_date = "2026-05-13"
# Forward-compat runtime floor: the dolphin family (E-Branchformer CTC +
# attention rescoring, keep-quantized executor) needs the 0.1.3 core runtime, so
# older builds surface it as "update to use" and refuse the pull rather than
# fetching a pack they cannot load. Author-set; never derived from the current
# build version.
min_core_version = "0.1.3"
min_cli_version = "0.1.3"

# Dolphin cn-dialect base (140M): the "base" tier of the same
# small.cn/cn-dialect-small architecture family -- identical char vocab +
# region-prompt scheme (same 14 registered dialect codes), only the
# encoder/decoder width shrinks (512 d_model / 8 heads / 2048 ffn / 6 layers vs
# small's 768/12/3072/12, cross-checked against each checkpoint's train.yaml).
# Flipped public 2026-07-07 on explicit user go-ahead, mirroring
# dolphin-cn-dialect-small's own launch.
["dolphin-cn-dialect-base"]
family = "dolphin"
hf_repo = "OpenASR/dolphin-cn-dialect-base"
release_public = true
size = "base"
registry_id = "dolphin-cn-dialect-base"
display_name = "Dolphin CN-Dialect Base"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/DataoceanAI/dolphin-cn-dialect-base/blob/main/README.md"
license_class = "permissive"
# Explicit Chinese-only override; see dolphin-cn-dialect-small's identical
# note above (same list, pinned per-model rather than inherited).
languages = [
    "zh", "zh-anhui", "zh-guangdong", "zh-hebei", "zh-hubei", "zh-jiangsu",
    "zh-ningxia", "zh-shaanxi", "zh-shandong", "zh-shanghai", "zh-shanxi",
    "zh-sichuan", "zh-tianjin", "zh-tw",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "fp16"
upstream_release_date = "2026-05-13"
# Same forward-compat runtime floor as dolphin-cn-dialect-small: needs the
# dim-derivation import generalization landing in this same change, so pin the
# next core release rather than claim compatibility with anything older.
min_core_version = "0.1.8"
min_cli_version = "0.1.8"

# Dolphin small (372M) + Dolphin base (140M): the multilingual siblings of the
# cn-dialect family. Card: "40 Eastern languages ... plus 22 Chinese dialects",
# SentencePiece BPE vocab (not char), and a two-level `<lang><region>` prompt
# (vs cn-dialect's fixed `<zh><region>`). `languages` lists the 40 officially
# supported languages (ISO 639-1 where one exists, 639-3 otherwise), collapsing
# the 22 Chinese dialects into the `zh` already among them (product's dialect-
# collapse rule -- Dolphin's OWN region-prompt granularity for those dialects is
# not separately advertised for the multilingual checkpoints, unlike the
# dedicated cn-dialect models). Cantonese (Dolphin's own `ct` code) is listed
# separately as `yue`, matching Dolphin's own languages.md, which treats it as
# a top-level language rather than a zh dialect.
# Flipped public 2026-07-08 as part of the 0.1.9 batch (HF repo already made
# public; catalog entry moves public:false -> public:true alongside the
# min_core_version = 0.1.9 core release).
["dolphin-small"]
family = "dolphin"
hf_repo = "OpenASR/dolphin-small"
release_public = true
# Bare `dolphin` resolves here (multilingual small tier) -- see
# dolphin-cn-dialect-small's note on the 2026-07 alias swap.
aliases = ["dolphin"]
pull_alias = "dolphin"
size = "small"
registry_id = "dolphin-small"
display_name = "Dolphin Small"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/DataoceanAI/dolphin-small/blob/main/README.md"
license_class = "permissive"
languages = [
    "ar", "az", "ba", "bn", "fa", "fil", "gu", "hi", "id", "ja", "jv", "kab",
    "kk", "km", "ko", "ks", "ky", "lo", "mn", "mr", "ms", "my", "ne", "or",
    "pa", "ps", "ru", "si", "su", "ta", "te", "tg", "th", "tl", "ug", "ur",
    "uz", "vi", "yue", "zh",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2026-05-13"
# Needs both the dim-derivation import generalization AND the new multilingual
# BPE tokenizer / two-level language-region prompt landing in this same change.
# Multilingual espnet front-end support (commit 91fc3d3) only lands in 0.1.9,
# not 0.1.8, so pin the runtime floor there. min_cli_version mirrors this: the
# published catalog entry has always shipped 0.1.9 here (set explicitly at
# publish time); pinning it in source too keeps a plain regenerate_all.sh
# reproducing the same floor instead of silently regressing to the 0.1.0
# catalog default.
min_core_version = "0.1.9"
min_cli_version = "0.1.9"

# Flipped public 2026-07-08 alongside dolphin-small, same 0.1.9 batch.
["dolphin-base"]
family = "dolphin"
hf_repo = "OpenASR/dolphin-base"
release_public = true
size = "base"
registry_id = "dolphin-base"
display_name = "Dolphin Base"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/DataoceanAI/dolphin-base/blob/main/README.md"
license_class = "permissive"
languages = [
    "ar", "az", "ba", "bn", "fa", "fil", "gu", "hi", "id", "ja", "jv", "kab",
    "kk", "km", "ko", "ks", "ky", "lo", "mn", "mr", "ms", "my", "ne", "or",
    "pa", "ps", "ru", "si", "su", "ta", "te", "tg", "th", "tl", "ug", "ur",
    "uz", "vi", "yue", "zh",
]
# q4_k dropped: 8.8% CER drift measured for dolphin-base (dolphin-small's q4_k
# is clean at 0.0 drift and stays).
quants = ["fp16", "q8_0"]
recommended_quant = "q8_0"
upstream_release_date = "2026-05-13"
# Multilingual espnet front-end support (commit 91fc3d3) only lands in 0.1.9,
# not 0.1.8, so pin the runtime floor there. min_cli_version mirrors this: the
# published catalog entry has always shipped 0.1.9 here (set explicitly at
# publish time); pinning it in source too keeps a plain regenerate_all.sh
# reproducing the same floor instead of silently regressing to the 0.1.0
# catalog default.
min_core_version = "0.1.9"
min_cli_version = "0.1.9"

# SenseVoiceSmall (FunASR SAN-M/DFSMN encoder + CTC head): multilingual
# zh/yue/en/ja/ko with model-side language id. Published PUBLIC to
# OpenASR/sensevoice-small: `release_public = true`. License is the FunASR
# Model License v1.1 -- a custom license permitting commercial use; never
# mislabel it Apache-2.0.
["sensevoice-small"]
family = "sensevoice"
hf_repo = "OpenASR/sensevoice-small"
release_public = true
aliases = ["sensevoice", "sensevoice-small"]
pull_alias = "sensevoice"
size = "small"
registry_id = "sensevoice-small"
display_name = "SenseVoice Small"
license_name = "FunASR Model License v1.1"
license_source = "https://github.com/modelscope/FunASR/blob/main/MODEL_LICENSE"
license_class = "permissive"
quants = ["fp16", "q8_0"]
recommended_quant = "fp16"
sort_weight = 900
upstream_release_date = "2024-07-04"
# Forward-compat runtime floor: the sensevoice family (SAN-M/FSMN encoder,
# keep-quantized executor) needs the next core runtime; older builds surface it
# as "update to use" and refuse the pull. Author-set.
min_core_version = "0.1.5"
min_cli_version = "0.1.5"

# Diarization support packs: not ASR models. Published support packs keep their
# catalog-declared variants. Speaker embedding/segmentation are
# language-agnostic; `languages` lists the product's validated domains.

["redimnet2-b6-cn"]
kind = "capability-pack"
family = "redimnet2"
hf_repo = "OpenASR/redimnet2-b6-cn"
release_public = true
size = "embedder"
registry_id = "redimnet2-b6-cn"
display_name = "ReDimNet2-B6 Speaker Embedder"
license_name = "MIT"
license_source = "https://github.com/PalabraAI/redimnet2/blob/main/LICENSE"
license_class = "permissive"
languages = ["en", "zh"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2026-03-04"
# Runtime support landed after the v0.1.23 tag on main (embedder graph +
# calibration + Voice ID v2). Current workspace version is still 0.1.23 and
# carries the code, so the public floor matches the workspace package version.
min_core_version = "0.1.23"
min_cli_version = "0.1.23"

["redimnet2-b6-cn".capability]
feature = "speaker-diarization"
role = "speaker-embedder"

# Optional WeSpeaker ResNet family (VoxCeleb English LM). Default Voice ID
# remains ReDimNet2-B6; these packs load only on an explicit preference.
# Staged until a GA that contains the WeSpeaker architecture; do not flip
# release_public while released 0.1.38 binaries cannot run these packs.
["wespeaker-voxceleb-resnet34-lm"]
kind = "capability-pack"
family = "wespeaker"
hf_repo = "OpenASR/wespeaker-voxceleb-resnet34-lm"
release_public = false
size = "resnet34"
registry_id = "wespeaker-voxceleb-resnet34-lm"
display_name = "WeSpeaker ResNet34 VoxCeleb LM"
license_name = "CC BY 4.0"
license_source = "https://github.com/wenet-e2e/wespeaker/blob/master/LICENSE"
license_class = "permissive"
languages = ["en"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2024-05-06"
min_core_version = "0.1.39"
min_cli_version = "0.1.38"

["wespeaker-voxceleb-resnet34-lm".capability]
feature = "speaker-diarization"
role = "speaker-embedder"

["wespeaker-voxceleb-resnet152-lm"]
kind = "capability-pack"
family = "wespeaker"
hf_repo = "OpenASR/wespeaker-voxceleb-resnet152-lm"
release_public = false
size = "resnet152"
registry_id = "wespeaker-voxceleb-resnet152-lm"
display_name = "WeSpeaker ResNet152 VoxCeleb LM"
license_name = "CC BY 4.0"
license_source = "https://github.com/wenet-e2e/wespeaker/blob/master/LICENSE"
license_class = "permissive"
languages = ["en"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2024-05-06"
min_core_version = "0.1.39"
min_cli_version = "0.1.38"

["wespeaker-voxceleb-resnet152-lm".capability]
feature = "speaker-diarization"
role = "speaker-embedder"

["wespeaker-voxceleb-resnet221-lm"]
kind = "capability-pack"
family = "wespeaker"
hf_repo = "OpenASR/wespeaker-voxceleb-resnet221-lm"
release_public = false
size = "resnet221"
registry_id = "wespeaker-voxceleb-resnet221-lm"
display_name = "WeSpeaker ResNet221 VoxCeleb LM"
license_name = "CC BY 4.0"
license_source = "https://github.com/wenet-e2e/wespeaker/blob/master/LICENSE"
license_class = "permissive"
languages = ["en"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2024-05-06"
min_core_version = "0.1.39"
min_cli_version = "0.1.38"

["wespeaker-voxceleb-resnet221-lm".capability]
feature = "speaker-diarization"
role = "speaker-embedder"

["wespeaker-voxceleb-resnet293-lm"]
kind = "capability-pack"
family = "wespeaker"
hf_repo = "OpenASR/wespeaker-voxceleb-resnet293-lm"
release_public = false
size = "resnet293"
registry_id = "wespeaker-voxceleb-resnet293-lm"
display_name = "WeSpeaker ResNet293 VoxCeleb LM"
license_name = "CC BY 4.0"
license_source = "https://github.com/wenet-e2e/wespeaker/blob/master/LICENSE"
license_class = "permissive"
languages = ["en"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2024-05-06"
min_core_version = "0.1.39"
min_cli_version = "0.1.38"

["wespeaker-voxceleb-resnet293-lm".capability]
feature = "speaker-diarization"
role = "speaker-embedder"


["pyannote-segmentation-3.0"]
kind = "capability-pack"
family = "pyannote-segmentation"
hf_repo = "OpenASR/pyannote-segmentation-3.0"
release_public = true
size = "segmenter"
registry_id = "pyannote-segmentation-3.0"
display_name = "pyannote Segmentation 3.0"
license_name = "MIT"
license_source = "https://huggingface.co/onnx-community/pyannote-segmentation-3.0"
license_class = "permissive"
languages = ["en", "zh"]
quants = ["f32"]
recommended_quant = "f32"
upstream_release_date = "2023-09-22"

["pyannote-segmentation-3.0".capability]
feature = "speaker-diarization"
role = "speaker-segmenter"

# Optional higher-accuracy speaker segmenter. The code/runtime is MIT, but the
# pinned Large-s80-md-v2 checkpoint is CC BY-NC 4.0 because some training corpora
# are research-only/non-commercial. The public pack therefore remains behind an
# explicit non-commercial license acceptance gate. segmentation-3.0 remains the
# permissive default/fallback and is never replaced by an implicit download.
["diarizen-large-s80-v2"]
kind = "capability-pack"
family = "diarizen-segmentation"
hf_repo = "OpenASR/diarizen-large-s80-v2"
release_public = true
size = "segmenter"
registry_id = "diarizen-large-s80-v2"
display_name = "DiariZen Large-s80-md-v2"
license_name = "CC BY-NC 4.0"
license_source = "https://huggingface.co/BUT-FIT/diarizen-wavlm-large-s80-md-v2/blob/f27b9ffbedcf422856d104ecee9b94be37ea578e/README.md"
license_class = "noncommercial"
languages = ["en", "zh"]
quants = ["fp16"]
recommended_quant = "fp16"
min_core_version = "0.1.28"
min_cli_version = "0.1.28"

["diarizen-large-s80-v2".capability]
feature = "speaker-diarization"
role = "speaker-segmenter"

["whisper-tiny"]
family = "whisper"
hf_repo = "OpenASR/whisper-tiny"
release_public = true
aliases = []
pull_alias = "whisper-tiny"
size = "tiny"
registry_id = "whisper-tiny"
display_name = "Whisper Tiny"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-tiny/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-base"]
family = "whisper"
hf_repo = "OpenASR/whisper-base"
release_public = true
aliases = []
pull_alias = "whisper-base"
size = "base"
registry_id = "whisper-base"
display_name = "Whisper Base"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-base/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-medium"]
family = "whisper"
hf_repo = "OpenASR/whisper-medium"
release_public = true
aliases = []
pull_alias = "whisper-medium"
size = "medium"
registry_id = "whisper-medium"
display_name = "Whisper Medium"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-medium/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-large-v3"]
family = "whisper"
hf_repo = "OpenASR/whisper-large-v3"
release_public = true
aliases = []
pull_alias = "whisper-large-v3"
size = "large"
registry_id = "whisper-large-v3"
display_name = "Whisper Large v3"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-large-v3/blob/main/README.md"
license_class = "permissive"
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2023-11-06"

["whisper-tiny.en"]
family = "whisper"
hf_repo = "OpenASR/whisper-tiny.en"
release_public = true
aliases = []
pull_alias = "whisper-tiny.en"
size = "tiny.en"
registry_id = "whisper-tiny.en"
display_name = "Whisper Tiny (English)"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-tiny.en/blob/main/README.md"
license_class = "permissive"
languages = ["en"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-base.en"]
family = "whisper"
hf_repo = "OpenASR/whisper-base.en"
release_public = true
aliases = []
pull_alias = "whisper-base.en"
size = "base.en"
registry_id = "whisper-base.en"
display_name = "Whisper Base (English)"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-base.en/blob/main/README.md"
license_class = "permissive"
languages = ["en"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-small.en"]
family = "whisper"
hf_repo = "OpenASR/whisper-small.en"
release_public = true
aliases = []
pull_alias = "whisper-small.en"
size = "small.en"
registry_id = "whisper-small.en"
display_name = "Whisper Small (English)"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-small.en/blob/main/README.md"
license_class = "permissive"
languages = ["en"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

["whisper-medium.en"]
family = "whisper"
hf_repo = "OpenASR/whisper-medium.en"
release_public = true
aliases = []
pull_alias = "whisper-medium.en"
size = "medium.en"
registry_id = "whisper-medium.en"
display_name = "Whisper Medium (English)"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/openai/whisper-medium.en/blob/main/README.md"
license_class = "permissive"
languages = ["en"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2022-09-21"

# FireRedASR2-AED (FireRedTeam Conformer encoder + Transformer decoder
# attention-based encoder-decoder, bilingual Mandarin + English + dialects).
# Same firered-aed family/executor and architecture as v1 (byte-identical
# per upstream source diff); v2 retrains on a larger vocab (8667 vs 7832
# tokens) and adds a post-hoc CTC branch this importer intentionally does not
# carry (no timestamp feature in this family yet). Only v2 ships -- v1
# (FireRedASR-AED-L) never went public and its catalog entry was replaced by
# this one rather than kept alongside it. Forward-compat floor pinned to the
# 0.1.9 release that ships the family, mirroring dolphin-cn-dialect-small's
# own min_core_version launch pattern. Flipped public 2026-07-08 as part of
# the 0.1.9 batch (release_public already true; catalog entry moves
# public:false -> public:true).
["firered-aed-l-v2"]
family = "firered-aed"
hf_repo = "OpenASR/firered-aed-l-v2"
release_public = true
aliases = ["firered-aed", "firered", "firered-aed-v2"]
pull_alias = "firered-aed-l-v2"
size = "l"
registry_id = "firered-aed-l-v2"
display_name = "FireRedASR2 AED-L"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/FireRedTeam/FireRedASR2-AED/blob/main/README.md"
license_class = "permissive"
# Explicit override: the family default from the generated firered-aed inventory row folds
# all Chinese dialects into "zh". FireRedASR2's own README lists 20+ Chinese
# dialects and arXiv:2603.10420 Table 2 benchmarks this AED checkpoint on 19
# public dialect/accent test sets (Sichuan x4, Shanghai, Henan, Hunan, Wuhan,
# Tianjin, ...), so this model is dialect-capable (see the generated family
# descriptor/inventory) and lists every registered dialect it was tested
# on. The paper's "Liaoning" test set is a Northeastern-Mandarin variety and
# is normalized to the registered zh-dongbei code rather than added as a
# separate zh-liaoning code.
languages = [
    "en", "nan", "wuu", "yue", "zh", "zh-anhui", "zh-dongbei", "zh-fujian",
    "zh-gansu", "zh-guizhou", "zh-hebei", "zh-henan", "zh-hubei", "zh-hunan",
    "zh-jiangxi", "zh-ningxia", "zh-shaanxi", "zh-shandong", "zh-shanghai",
    "zh-shanxi", "zh-sichuan", "zh-tianjin", "zh-yunnan",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q4_k"
sort_weight = 912
recommended = true
upstream_release_date = "2026-02-12"
min_core_version = "0.1.9"
min_cli_version = "0.1.9"

# FireRedASR2-LLM (FireRedTeam Conformer encoder + adapter feeding a LoRA-merged
# Qwen2-7B-Instruct decoder -- the Encoder-Adapter-LLM member of FireRedASR2,
# distinct from the smaller AED member firered-aed-l-v2). Its own runtime
# family/architecture (`firered2-llm` / firered-llm-conformer-adapter-qwen2 in
# arch/mod.rs), NOT the firered-aed family. Bilingual Mandarin + English via the
# Qwen2 BPE vocab with no language-selection prompt token (FixedMultilingual).
# Only the q4_k pack ships: on Metal the fp16/q8_0 7B decoder OOMs this M1's
# working-set ceiling (see the perf commit that guards it), and q4_k is the one
# quant benchmarked at RTF < 1 on Metal. Published to a PUBLIC HF repo and
# listed public in the 0.1.17 catalog flip. min_core_version / min_cli_version
# pinned to the 0.1.17 release that first ships the runtime.
["firered2-llm"]
family = "firered2-llm"
hf_repo = "OpenASR/firered2-llm"
release_public = true
aliases = ["firered-llm", "firered2"]
pull_alias = "firered2-llm"
size = "8b"
registry_id = "firered2-llm"
display_name = "FireRedASR2 LLM"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/FireRedTeam/FireRedASR2-LLM"
license_class = "permissive"
# Explicit per-model list, kept in lockstep with the generated firered2-llm inventory row:
# bilingual Mandarin + English, mirroring the Qwen2 BPE decoder's
# FixedMultilingual { languages: &["zh", "en"] } arch descriptor.
languages = ["en", "zh"]
quants = ["q4_k"]
recommended_quant = "q4_k"
sort_weight = 890
upstream_release_date = "2026-02-12"
min_core_version = "0.1.17"
min_cli_version = "0.1.17"

# MiMo-V2.5-ASR (Xiaomi MiMo-Audio-Tokenizer encoder + 8-level RVQ + input-local
# transformer feeding a 36L Qwen2 decoder backbone). Its own runtime family/
# architecture (`mimo-asr` in arch/mod.rs). The Qwen2 BPE vocab covers the
# upstream's trained Mandarin, English, and Cantonese (`yue`) with no
# language-selection prompt token (FixedMultilingual); the runtime strips the
# `<chinese>`/`<english>` auto-emitted language markers before returning text.
# Only the q4_k pack ships: the fp16/q8_0 packs OOM Metal's working-set ceiling
# on this M1 (same class as firered2-llm above), and q4_k is the one quant
# benchmarked at RTF < 1 on Metal. MIT-licensed upstream. Published to a PUBLIC
# HF repo and listed public in the 0.1.17 catalog flip. min_core_version /
# min_cli_version pinned to the 0.1.17 release that first ships the runtime.
["mimo-v2.5-asr"]
family = "mimo-asr"
hf_repo = "OpenASR/mimo-v2.5-asr"
release_public = true
aliases = ["mimo", "mimo-v2.5"]
pull_alias = "mimo-v2.5-asr"
size = "8b"
registry_id = "mimo-v2.5-asr"
display_name = "MiMo-V2.5-ASR"
license_name = "MIT"
license_source = "https://huggingface.co/XiaomiMiMo/MiMo-V2.5-ASR/blob/main/README.md"
license_class = "permissive"
# Explicit per-model list, kept in lockstep with the generated mimo-asr inventory row:
# Mandarin + English + Cantonese, mirroring the Qwen2 BPE decoder's
# FixedMultilingual { languages: &["zh", "en", "yue"] } arch descriptor. "yue"
# is a base ISO 639-3 code (Cantonese), not a dialect subtag.
languages = ["en", "yue", "zh"]
quants = ["q4_k"]
recommended_quant = "q4_k"
sort_weight = 888
min_core_version = "0.1.17"
min_cli_version = "0.1.17"

# Qwen3-ForcedAligner-0.6B: a capability pack for the `word-timestamps`
# feature (`--word-timestamps=aligned`), mirroring the redimnet/pyannote
# capability-pack entries above. Shares its thinker tensor layout with
# qwen3-asr but is registered as its own family/architecture id
# (`qwen3-forced-aligner`) since its NAR (single forward pass, argmax at
# `<timestamp>` positions) decode policy is materially different from the
# qwen3-asr autoregressive runtime -- see
# crates/openasr-core/src/models/qwen/forced_aligner_import.rs.
# release_public=true allows the public projection only after public_gate.py
# verifies the same anonymous HF repository and pinned artifacts that end
# users download. Repository visibility alone never bypasses that gate.
["qwen3-forced-aligner-0.6b"]
kind = "capability-pack"
family = "qwen3-forced-aligner"
hf_repo = "OpenASR/qwen3-forced-aligner-0.6b"
release_public = true
size = "0.6b"
registry_id = "qwen3-forced-aligner-0.6b"
display_name = "Qwen3-ForcedAligner 0.6B"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/Qwen/Qwen3-ForcedAligner-0.6B/blob/c7cbfc2048c462b0d63a45797104fc9db3ad62b7/LICENSE"
license_class = "permissive"
# Language-agnostic support pack (like the diarization embedder/segmenter
# above): it aligns whatever text/audio pair a base ASR model already
# produced, so there is no meaningful "family default" language list. This
# override just documents the languages exercised by local verification so
# far (see the align-text word-splitting support for en/zh).
languages = ["en", "zh"]
#
# Forced alignment makes discrete decisions over 80 ms timestamp bins. The
# public q4_k name follows the catalog-wide tier vocabulary; it does not mean
# that every matrix is physically Q4_K. The audio encoder, token embedding,
# and timestamp head remain Q8_0, while only policy-eligible text-decoder
# matrices use Q4_K. Pack verification replays that exact per-tensor policy so
# older all-Q4 artifacts and mislabeled mixed packs fail closed.
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q4_k"
upstream_release_date = "2026-01-28"
# q4_k support and its per-tensor trust-boundary validation land after 0.1.31.
# The whole capability-pack entry is version-gated because the signed catalog
# has one compatibility floor per model, not per quant variant.
min_core_version = "0.1.32"
min_cli_version = "0.1.32"

["qwen3-forced-aligner-0.6b".capability]
feature = "word-timestamps"
role = "forced-aligner"

# Punctuation-restoration support pack: a chinese-lert-base BERT token
# classifier (upstream FireRedTeam/FireRedPunc) that adds Chinese full-width
# marks to an unpunctuated family's transcript in a finalize-only post-process.
# Not an ASR model. Chinese-only by construction (the released label set is the
# 5 classes none/，。？！). Published public, like the dolphin multilingual
# tiers above.
["firered-punc"]
kind = "capability-pack"
family = "firered-punc"
hf_repo = "OpenASR/firered-punc"
release_public = true
size = "base"
registry_id = "firered-punc"
display_name = "FireRedPunc"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/FireRedTeam/FireRedPunc"
license_class = "permissive"
# Chinese-only by construction: the released label set cannot emit English
# half-width marks, so this documents the single supported recognition language
# rather than inheriting a family default.
languages = ["zh"]
quants = ["fp16"]
recommended_quant = "fp16"
upstream_release_date = "2026-02-12"
# min_cli_version mirrors min_core_version: the published catalog entry has
# always shipped 0.1.11 here (set explicitly at publish time); pinning it in
# source too keeps a plain regenerate_all.sh reproducing the same floor
# instead of silently regressing it to the 0.1.0 catalog default.
min_core_version = "0.1.11"
min_cli_version = "0.1.11"

["firered-punc".capability]
feature = "punctuation"
role = "punctuation-restorer"

# MOSS-Transcribe-Diarize (OpenMOSS-Team/MOSS-Transcribe-Diarize, 0.9B): a
# Whisper-Medium-configuration audio encoder + a small MLP/LayerNorm adaptor
# bridge feeding a Qwen3-0.6B-style decoder. Its own runtime family/
# architecture (`moss-transcribe-diarize` in arch/mod.rs). The decoder emits
# `[start][Sxx]text[end]` transcript text directly (self_diarizes = true in
# the arch descriptor) via free-text instruction-following, with no
# language-selection prompt token (LanguageFamilyHint::SelfDetectsRejectsHint,
# same shape as qwen3-asr) -- language_mode is therefore "detect_implicit".
# Public release state: `release_public` projects this source row into the
# generated signed/public catalogs, which pin the immutable HF revision and all
# three quant artifacts. Future artifact, hash, or catalog changes still require
# a completed release audit and explicit publication authorization; this boolean
# records current state and never grants a later publish action by itself.
["moss-transcribe-diarize"]
family = "moss-transcribe-diarize"
hf_repo = "OpenASR/moss-transcribe-diarize"
release_public = true
size = "0.9b"
registry_id = "moss-transcribe-diarize"
display_name = "MOSS-Transcribe-Diarize"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/OpenMOSS-Team/MOSS-Transcribe-Diarize/blob/main/LICENSE"
license_class = "permissive"
aliases = ["moss", "moss-td"]
pull_alias = "moss-transcribe-diarize"
# Explicit override, deliberately conservative: the upstream model card only
# claims "50+ languages" without enumerating them. The only languages with a
# concrete upstream evidentiary source are (a) Mandarin Chinese, from the
# model's own primary objective-evaluation benchmarks (AISHELL-4, Alimeeting
# -- both Mandarin meeting corpora) and this repo's own dev-pack golden
# transcripts, and (b) the 14 languages the upstream README names by name for
# the model's 1st-place 2nd MLC-SLM Challenge result (English, French,
# German, Italian, Portuguese, Spanish, Japanese, Korean, Russian, Thai,
# Vietnamese, Tagalog, Urdu, Turkish). Do not expand this list to the
# unenumerated "50+" claim without a concrete per-language source.
languages = [
    "de", "en", "es", "fr", "it", "ja", "ko", "pt", "ru", "th", "tl", "tr",
    "ur", "vi", "zh",
]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2026-07-09"
min_core_version = "0.1.24"
min_cli_version = "0.1.24"

# Fun-ASR-Nano (FunAudioLLM/Fun-ASR-Nano-2512, ~0.8B): FunASR SAN-M/DFSMN audio
# encoder + 2-layer transformer adaptor + stock Qwen3-0.6B decoder. Its own
# runtime family/architecture (`funasr-nano` / `funasr-nano-sanm-adapter-qwen3`
# in arch/mod.rs). FixedMultilingual zh+en (no language-selection prompt
# token); emits_punctuation unclaimed (None). Pack import is the Rust
# funasr_nano::package_import importer (`openasr model-pack import
# funasr-nano`) over the funasr_nano_pt_to_safetensors.py staged source.
# Public catalog entry; HF packs live at OpenASR/funasr-nano. languages is
# an explicit override, lockstep with
# FixedMultilingual { languages: &["zh","en"] } in arch/mod.rs (upstream card
# also mentions Japanese; the runtime arch only ships the zh+en fixed set).
["funasr-nano"]
family = "funasr-nano"
hf_repo = "OpenASR/funasr-nano"
release_public = true
size = "0.8b"
registry_id = "funasr-nano"
display_name = "Fun-ASR-Nano"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/FunAudioLLM/Fun-ASR-Nano-2512/blob/main/LICENSE"
license_class = "permissive"
aliases = ["funasr", "fun-asr-nano"]
pull_alias = "funasr-nano"
# Explicit override, lockstep with FixedMultilingual { languages: &["zh","en"] }
# in arch/mod.rs. Upstream card also mentions Japanese; the runtime arch only
# ships the zh+en fixed set, so the catalog follows the arch, not marketing.
languages = ["en", "zh"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2025-12-01"
# Runtime first ships with core 0.1.27; catalog min_* floors match.
min_core_version = "0.1.27"
min_cli_version = "0.1.27"

# IBM Granite Speech 4.1 2B (ibm-granite/granite-speech-4.1-2b): 16-layer
# Conformer encoder + Q-Former projector + 2B Granite decoder. Its own runtime
# family/architecture (`granite-speech` in arch/mod.rs). SelfDetectsRejectsHint
# (detect_implicit) -- free-text instruction-following, no language token;
# emits_punctuation Some(true) (observed on goldens). Pack import is
# CoreConvert `convert_local_granite_speech_source_to_runtime_pack`. Public
# catalog entry; HF packs live at OpenASR/granite-speech-4.1-2b. languages is
# an explicit override from the upstream trained-language list.
["granite-speech-4.1-2b"]
family = "granite-speech"
hf_repo = "OpenASR/granite-speech-4.1-2b"
release_public = true
size = "2b"
registry_id = "granite-speech-4.1-2b"
display_name = "Granite Speech 4.1 2B"
license_name = "Apache-2.0"
license_source = "https://huggingface.co/ibm-granite/granite-speech-4.1-2b/blob/main/LICENSE"
license_class = "permissive"
aliases = ["granite-speech", "granite"]
pull_alias = "granite-speech"
# Explicit override from the upstream model card's trained-language list
# (English, French, German, Spanish, Portuguese, Japanese). Do not expand
# without a concrete per-language source.
languages = ["de", "en", "es", "fr", "ja", "pt"]
quants = ["fp16", "q8_0", "q4_k"]
recommended_quant = "q8_0"
upstream_release_date = "2025-05-01"
min_core_version = "0.1.27"
min_cli_version = "0.1.27"
